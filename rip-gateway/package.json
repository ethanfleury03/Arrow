{
  "name": "rip-gateway",
  "version": "1.1.0",
  "description": "HTTP Gateway for RIP-UI - receives print jobs from laptops and forwards to Linux printhead backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "set NODE_ENV=development && node server.js",
    "install-service": "node install-windows-service.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
